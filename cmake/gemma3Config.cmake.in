@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Required dependencies
if(NOT WIN32)
    find_dependency(Threads)
endif()

# Optional dependencies based on build configuration
if(@GEMMA3_USE_BLAS@)
    find_dependency(OpenBLAS)
endif()

if(@GEMMA3_USE_WEBGPU@)
    find_dependency(WebGPU)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/gemma3Targets.cmake")

check_required_components(gemma3)
